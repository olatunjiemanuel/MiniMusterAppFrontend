# MiniMusterAppFrontend

A basic frontend to test the .Net API endpoint 

## 🧰 Features

- Mobile UI for managing attendance/muster data (viewing entries, creating, updating)  
- Connects to the backend API (e.g., your MiniMusterAPI) for data operations  
- Built using React + Vite 
- Clean UI/UX structure, reusable components, state management (use redux/context/hooks)  
- Ready for extension into production (authentication, offline cache, push notifications)  

## 🧩 Getting Started

### Prerequisites

- Node.js installed  
- Yarn or npm package manager  
- React Native development setup (e.g., Android Studio, Xcode if targeting iOS)  
- (Optional) Expo CLI, if using Expo workflow  

### Setup & Run

1. Clone the repository  
   ```bash
   git clone https://github.com/olatunjiemanuel/MiniMusterAppFrontend.git
   cd MiniMusterAppFrontend
2. npm install    # or yarn
3. npm run dev
